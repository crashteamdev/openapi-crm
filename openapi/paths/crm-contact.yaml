post:
  summary: Добавить контакт
  description: |
    В передаваемый список полей добавляются уникальные, для каждого клиента, поля которые настраиваются в системе
  operationId: createContact
  security:
    - bearer: []
  tags:
    - contact
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../components/parameters/contact.yaml'
  responses:
        '201':
          description: Контакт создан
          content: {}
