description: Create demo lead
required:
  - userIdentity
  - userPhoneNumber
  - userEmail
allOf:
  - $ref: ./CreateLead.yaml
  - type: object
    properties:
      userIdentity:
        $ref: '../UserIdentity.yaml'
      userPhoneNumber:
        $ref: '../Phone.yaml'
      userEmail:
        $ref: '../Email.yaml'
      utmTags:
        $ref: '../UtmTags.yaml'
